# Dockerfile.dev (Updated)
# Use a Debian-based slim image for better native module compatibility
FROM node:20-slim

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the project
COPY . .

CMD ["npm", "run", "dev"]