export function Toolbar() {
  return (
    <div className="flex gap-2 p-2 bg-gray-100 border-b items-center">
      {/* toolbar left intentionally minimal; aspect ratio moved to canvas top-right */}
    </div>
  );
}
